[general]
user = marche
group = marchegroup
logdir = /tmp/log
piddir = /tmp/pid
interfaces = xmlrpc, wsserver

[interface.xmlrpc]
user = legacy
passwd = legacy

[auth.simple]
user = simple
passwd = simple
